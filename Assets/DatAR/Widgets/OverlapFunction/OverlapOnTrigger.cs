using System;
using System.Collections.Generic;
using System.Linq;
using Cysharp.Threading.Tasks;
using DatAR.Auxiliary.SharedScripts;
using DatAR.DataModels.Misc;
using DatAR.DataModels.Passables;
using DatAR.DataModels.Resources;
using Newtonsoft.Json;
using TMPro;
using UniRx;
using UnityEngine;

public class OverlapOnTrigger : MonoBehaviour
{

    // public GameObject Overlapobject;
    //public GameObject otherobject;

    /*

    private ColorService _colorService;
    private material inFilterRangeColor;
    Renderer rend;



    private void Awake()
    {
        var services = GameObject.Find("Services");
        _colorService = services.GetComponent<ColorService>();
        //_sparqlService = services.GetComponent<SparqlService>();
    }

    void start() 
    {
        rend = GetComponent<Renderer>();
        rend.enable = true;
        rend.sharedMaterial = _colorService.inFilterRangeColor.color;


    }

    void OnTriggerEnter(Collider collider)
    {

       // activeGameObject01 = GameObject.FindWithTag("Sphere01");

        if (collider.gameObject.tag == "Player")
        {

         //   rend.sharedMaterial = _colorService.OverlapColor.color
            // otherobject.transform.position = transform.TransformPoint(0, 2, 1);
            // print("Item picked up");
            //   Overlapobject.SetActive(true);

        }
    }*/
}
